const express = require('express');
const router = express.Router();

router.get('/login', function(req, res, next) {
  // res.redirect("http://localhost/auth/login.php")
});

module.exports = router;
